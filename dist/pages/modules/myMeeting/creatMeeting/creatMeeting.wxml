
    <view class="body">
        <view class="top-title">
            <text>创建会议</text>
        </view>
        <view class="main">
                
<view>
     <view class="textarea"  wx:if="{{$Title$isShow}}">
        <view>
            <text>{{$Title$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Title$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Title$input.name}}" bindfocus="$Title$bindfocus" bindblur="$Title$bindblur" placeholder="请输入{{$Title$input.title}}" bindinput="$Title$bindinput" maxlength="-1" value="{{$Title$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Title$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Title$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Title$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Title$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Title$input.name}}" bindfocus="$Title$bindfocus" bindblur="$Title$bindblur" placeholder="请输入{{$Title$input.title}}" bindinput="$Title$bindinput" disabled="{{$Title$input.disabled}}" value="{{$Title$inputValue}}" type="{{$Title$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Title$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
    <view class="option">
        <view>
            <text class="title">{{$Category$options.title}}</text>
        </view>
        <picker bindchange="$Category$bindPickerChange" value="{{$Category$index}}" range="{{$Category$options.data}}" range-key="{{$Category$options.key}}" name="{{$Category$options.name}}" disabled="{{$Category$options.disabled}}">
            <view class="picker">
                <view>
                    <text>{{$Category$options.data[$Category$index][$Category$options.key]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Category$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
<view>
     <view class="textarea"  wx:if="{{$Subject$isShow}}">
        <view>
            <text>{{$Subject$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Subject$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Subject$input.name}}" bindfocus="$Subject$bindfocus" bindblur="$Subject$bindblur" placeholder="请输入{{$Subject$input.title}}" bindinput="$Subject$bindinput" maxlength="-1" value="{{$Subject$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Subject$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Subject$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Subject$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Subject$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Subject$input.name}}" bindfocus="$Subject$bindfocus" bindblur="$Subject$bindblur" placeholder="请输入{{$Subject$input.title}}" bindinput="$Subject$bindinput" disabled="{{$Subject$input.disabled}}" value="{{$Subject$inputValue}}" type="{{$Subject$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Subject$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
    <view class="option">
        <view>
            <text class="title">{{$ClientId$options.title}}</text>
        </view>
        <picker bindchange="$ClientId$bindPickerChange" value="{{$ClientId$index}}" range="{{$ClientId$options.data}}" range-key="{{$ClientId$options.key}}" name="{{$ClientId$options.name}}" disabled="{{$ClientId$options.disabled}}">
            <view class="picker">
                <view>
                    <text>{{$ClientId$options.data[$ClientId$index][$ClientId$options.key]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$ClientId$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$CaseId$options.title}}</text>
        </view>
        <picker bindchange="$CaseId$bindPickerChange" value="{{$CaseId$index}}" range="{{$CaseId$options.data}}" range-key="{{$CaseId$options.key}}" name="{{$CaseId$options.name}}" disabled="{{$CaseId$options.disabled}}">
            <view class="picker">
                <view>
                    <text>{{$CaseId$options.data[$CaseId$index][$CaseId$options.key]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$CaseId$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="tui-picker-content">
        <view>{{$StartTime$timeObj.title}}</view>
        <picker mode="multiSelector" value="{{$StartTime$dateTime}}" bindchange="$StartTime$changeDateTime" range="{{$StartTime$dateTimeArray}}" name="{{$StartTime$timeObj.name}}" disabled="{{$StartTime$timeObj.isChecked}}" bindcolumnchange="$StartTime$changeDateTimeColumn">
            <view class="tui-picker-detail" style="background:{{$StartTime$timeObj.isChecked?'#dfdfdf':''}}">
                 {{$StartTime$dateData[0]+'/'+$StartTime$dateData[1]+'/'+$StartTime$dateData[2]+' '+$StartTime$dateData[3]+':'+$StartTime$dateData[4]+':'+$StartTime$dateData[5]}}
                 <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
    </view>

                
    <view class="tui-picker-content">
        <view>{{$EndTime$timeObj.title}}</view>
        <picker mode="multiSelector" value="{{$EndTime$dateTime}}" bindchange="$EndTime$changeDateTime" range="{{$EndTime$dateTimeArray}}" name="{{$EndTime$timeObj.name}}" bindcolumnchange="$EndTime$changeDateTimeColumn">
            <view class="tui-picker-detail">
                  {{$EndTime$dateData[0]+'/'+$EndTime$dateData[1]+'/'+$EndTime$dateData[2]+' '+$EndTime$dateData[3]+':'+$EndTime$dateData[4]+':'+$EndTime$dateData[5]}}
                 <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$Moderator$options.title}}</text>
        </view>
        <picker bindchange="$Moderator$bindPickerChange" value="{{$Moderator$index}}" range="{{$Moderator$options.data}}" range-key="{{$Moderator$options.key}}" name="{{$Moderator$options.name}}" disabled="{{$Moderator$options.disabled}}">
            <view class="picker">
                <view>
                    <text>{{$Moderator$options.data[$Moderator$index][$Moderator$options.key]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Moderator$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$Linker$options.title}}</text>
        </view>
        <picker bindchange="$Linker$bindPickerChange" value="{{$Linker$index}}" range="{{$Linker$options.data}}" range-key="{{$Linker$options.key}}" name="{{$Linker$options.name}}" disabled="{{$Linker$options.disabled}}">
            <view class="picker">
                <view>
                    <text>{{$Linker$options.data[$Linker$index][$Linker$options.key]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Linker$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                <view class="option">
                    <view>
                        <text class="title">参与人</text>
                    </view>
                    <picker bindchange="bindPickerChange" value="{{Participant.index[Participant.index.length-1]}}" range="{{Participant.displayText}}" name="Participant" data-wpybindpickerchange-a="Participant">
                        <view class="picker">
                            <view style="display:flex;flex-wrap: wrap">
                                <block wx:for="{{Participant.index}}" wx:for-index="index" wx:for-item="item" wx:key="index">
                                    <view class="part-item">
                                        <text>{{Participant.displayText[item]}}</text>
                                        <icon class="iconfont icon-zirenwushanchu1" style="font-size:48rpx" catchtap="deletePItem" data-wpydeletepitem-a="{{index}}"/>
                                    </view>
                                </block>
                            </view>
                            <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                        </view>
                       
                    </picker>
                     <view  wx:if="{{Participant.ParticipantWarning}}" style="color:#e20000">
                            <text>此字段是必填字段</text>
                    </view>
                </view>
                
<view>
     <view class="textarea"  wx:if="{{$Visitor$isShow}}">
        <view>
            <text>{{$Visitor$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Visitor$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Visitor$input.name}}" bindfocus="$Visitor$bindfocus" bindblur="$Visitor$bindblur" placeholder="请输入{{$Visitor$input.title}}" bindinput="$Visitor$bindinput" maxlength="-1" value="{{$Visitor$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Visitor$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Visitor$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Visitor$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Visitor$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Visitor$input.name}}" bindfocus="$Visitor$bindfocus" bindblur="$Visitor$bindblur" placeholder="请输入{{$Visitor$input.title}}" bindinput="$Visitor$bindinput" disabled="{{$Visitor$input.disabled}}" value="{{$Visitor$inputValue}}" type="{{$Visitor$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Visitor$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                <view class="option-image">
                    <picker bindchange="bindPickerChange" value="{{Level.index}}" range="{{Level.displayText}}" name="Level" data-wpybindpickerchange-a="Level">
                        <view class="picker">
                            <view>
                            <icon class="iconfont icon-jibie1" style="color:#7a7a7a;font-size:40rpx"/>
                        </view>
                        <view>
                            <view>
                                <text>{{Level.index>=0?Level.displayText[Level.index]:Level.title}}</text>
                            </view>
                            <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2;margin-left:20rpx;"/>
                        </view>
                        </view>
                    </picker>
                </view>
                <view class="option-image">
                    <picker bindchange="bindPickerChange" value="{{meetingRemind.index}}" range="{{meetingRemind.displayText}}" name="meetingRemind" data-wpybindpickerchange-a="meetingRemind">
                        <view class="picker">
                            <view>
                            <icon class="iconfont icon-tixingkaiting" style="color:#7a7a7a;font-size:40rpx"/>
                        </view>
                        <view>
                            <view>
                                <text>{{meetingRemind.index>=0?meetingRemind.displayText[meetingRemind.index]:meetingRemind.title}}</text>
                            </view>
                            <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2;margin-left:20rpx;"/>
                        </view>
                        </view>
                        
                    </picker>
                </view>
                <view class="checked">
                    <view>
                        <checkbox value="{{IsMarkPrivacy[0].checked}}" id="Privacy" checked="{{IsMarkPrivacy[0].checked}}" bindtap="isChecked" color="#5d73fa"/>{{IsMarkPrivacy[0].title}}
                    </view>
                    <view>
                        <checkbox id="IsMark" value="{{IsMarkPrivacy[1].checked}}" checked="{{IsMarkPrivacy[1].checked}}" bindtap="isChecked" color="#5d73fa"/>{{IsMarkPrivacy[1].title}}
                    </view>
                    
                </view>
                
<view>
     <view class="textarea"  wx:if="{{$Remark$isShow}}">
        <view>
            <text>{{$Remark$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Remark$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Remark$input.name}}" bindfocus="$Remark$bindfocus" bindblur="$Remark$bindblur" placeholder="请输入{{$Remark$input.title}}" bindinput="$Remark$bindinput" maxlength="-1" value="{{$Remark$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Remark$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Remark$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Remark$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Remark$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Remark$input.name}}" bindfocus="$Remark$bindfocus" bindblur="$Remark$bindblur" placeholder="请输入{{$Remark$input.title}}" bindinput="$Remark$bindinput" disabled="{{$Remark$input.disabled}}" value="{{$Remark$inputValue}}" type="{{$Remark$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Remark$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
<view>
     <view class="textarea"  wx:if="{{$Prepare$isShow}}">
        <view>
            <text>{{$Prepare$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Prepare$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Prepare$input.name}}" bindfocus="$Prepare$bindfocus" bindblur="$Prepare$bindblur" placeholder="请输入{{$Prepare$input.title}}" bindinput="$Prepare$bindinput" maxlength="-1" value="{{$Prepare$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Prepare$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Prepare$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Prepare$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Prepare$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Prepare$input.name}}" bindfocus="$Prepare$bindfocus" bindblur="$Prepare$bindblur" placeholder="请输入{{$Prepare$input.title}}" bindinput="$Prepare$bindinput" disabled="{{$Prepare$input.disabled}}" value="{{$Prepare$inputValue}}" type="{{$Prepare$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Prepare$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                <image src="{{'../../../../images/next.png'}}" mode="scaleToFill" lazy-load="true" class="addImage" style="opacity:{{addOpacity}} ;" bindtouchstart="touchStart" bindtouchend="touchEnd"/>
        </view>
    </view>