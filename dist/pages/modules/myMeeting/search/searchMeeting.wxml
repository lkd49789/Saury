
    <view class="container">
        <view class="keyword-search"  wx:if="{{showPage}}">
            <view class="search_top">
                <view class="search_input">
                    <icon class="iconfont icon-sousuo1 input_sousuo" style="color:#5d73fa;font-size:24px;"/>
                    <input value="{{searchClentValue}}" type="text" confirm-type="search" placeholder="会议标题/内容描述" placeholder-class="placeholder" maxlength="30" bindconfirm="submitSearch" focus="true">
                    </input>
                </view>
                <view class="top_right_btn">
                    <icon class="iconfont icon-gaojisousuo" style="color:#5d73fa;font-size:35px;" bindtap="showPage"/>
                </view>
            </view>
            <view class="search_history">
                <view class="history_title">
                    <text>历史搜索</text>
                    <icon class="iconfont icon-shanchu2" style="color:#7a7a7a;font-size:35rpx;" catchtap="deletItemAll" wx:if="{{history_keyWord_case.length!==0}}"/>
                </view>
                <view class="history_content">
                    <block wx:for="{{history_keyWord_case}}" wx:for-index="index" wx:for-item="item" wx:key="index">
                        <view bindtap="history" bindlongpress="longTap" data-wpyhistory-a="{{item}}" data-wpylongtap-a="{{index}}">
                            <text>{{item}}</text>
                            <icon class="iconfont icon-zirenwushanchu" style="color:#dfdfdf;font-size:30rpx;" wx:if="{{isShowArray[index]}}" catchtap="deletItem" data-wpydeletitem-a="{{index}}"/>
                        </view>
                    </block>
                </view>
            </view>
        </view>
        <view class="advanced-search-input-panel"  wx:else="{{showPage}}">
            <view class="advanced-search-title">
                <text>高级搜索</text>
                <icon class="iconfont icon-zirenwushanchu" style="color:#7a7a7a;font-size:40rpx;" bindtap="showPage"/>
            </view>
            <view class="advanced-search-content">
                <view class="tui-picker-content">
                    <view>会议时间</view>
                    <view style="display:flex;align-items:center;">
                        <picker id="startMeetingDate" mode="date" value="{{searchData.MeetingTime.StartDate}}" start="2000-01-01" end="2050-12-31" bindchange="bindDateChange" style="flex:1">
                            <view class="tui-picker-detail">
                                {{searchData.MeetingTime.StartDate||'请选择'}}
                                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                            </view>
                        </picker>
                        <text style="margin:0 20rpx">至</text>
                        <picker id="endMeetingDate" mode="date" value="{{searchData.MeetingTime.EndDate}}" start="2000-01-01" end="2050-12-31" bindchange="bindDateChange" style="flex:1">
                            <view class="tui-picker-detail">
                                {{searchData.MeetingTime.EndDate||'请选择'}}
                                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                            </view>
                        </picker>
                    </view>
                </view>
                <view class="tui-picker-content">
                    <view>创建时间</view>
                    <view style="display:flex;align-items:center;">
                        <picker id="startCreateDate" mode="date" value="{{searchData.CreationTime.StartDate}}" start="2000-01-01" end="2050-12-31" bindchange="bindDateChange" style="flex:1">
                            <view class="tui-picker-detail">
                                {{searchData.CreationTime.StartDate||'请选择'}}
                                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                            </view>
                        </picker>
                        <text style="margin:0 20rpx">至</text>
                        <picker id="endCreateDate" mode="date" value="{{searchData.CreationTime.EndDate}}" start="2000-01-01" end="2050-12-31" bindchange="bindDateChange" style="flex:1">
                            <view class="tui-picker-detail">
                                {{searchData.CreationTime.EndDate||'请选择'}}
                                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                            </view>
                        </picker>
                    </view>
                </view>
                
<view>
     <view class="textarea"  wx:if="{{$Subject$isShow}}">
        <view>
            <text>{{$Subject$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Subject$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Subject$input.name}}" bindfocus="$Subject$bindfocus" bindblur="$Subject$bindblur" placeholder="请输入{{$Subject$input.title}}" bindinput="$Subject$bindinput" maxlength="-1" value="{{$Subject$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Subject$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Subject$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Subject$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Subject$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Subject$input.name}}" bindfocus="$Subject$bindfocus" bindblur="$Subject$bindblur" placeholder="请输入{{$Subject$input.title}}" bindinput="$Subject$bindinput" disabled="{{$Subject$input.disabled}}" value="{{$Subject$inputValue}}" type="{{$Subject$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Subject$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
<view>
     <view class="textarea"  wx:if="{{$Title$isShow}}">
        <view>
            <text>{{$Title$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Title$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Title$input.name}}" bindfocus="$Title$bindfocus" bindblur="$Title$bindblur" placeholder="请输入{{$Title$input.title}}" bindinput="$Title$bindinput" maxlength="-1" value="{{$Title$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Title$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Title$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Title$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Title$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Title$input.name}}" bindfocus="$Title$bindfocus" bindblur="$Title$bindblur" placeholder="请输入{{$Title$input.title}}" bindinput="$Title$bindinput" disabled="{{$Title$input.disabled}}" value="{{$Title$inputValue}}" type="{{$Title$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Title$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
    <view class="option">
        <view>
            <text class="title">{{$Status$options.title}}</text>
        </view>
        <picker bindchange="$Status$bindPickerChange" value="{{$Status$index}}" range="{{$Status$options.displayText}}" name="{{$Status$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Status$options.displayText[$Status$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Status$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$Category$options.title}}</text>
        </view>
        <picker bindchange="$Category$bindPickerChange" value="{{$Category$index}}" range="{{$Category$options.displayText}}" name="{{$Category$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Category$options.displayText[$Category$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Category$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$Level$options.title}}</text>
        </view>
        <picker bindchange="$Level$bindPickerChange" value="{{$Level$index}}" range="{{$Level$options.displayText}}" name="{{$Level$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Level$options.displayText[$Level$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Level$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$MeetingRoomId$options.title}}</text>
        </view>
        <picker bindchange="$MeetingRoomId$bindPickerChange" value="{{$MeetingRoomId$index}}" range="{{$MeetingRoomId$options.displayText}}" name="{{$MeetingRoomId$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$MeetingRoomId$options.displayText[$MeetingRoomId$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$MeetingRoomId$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                <image class="btn-sub" src="../../../../images/test.png" mode="scaleToFill" lazy-load="false" catchtap="advancedSearchSubmit"/>
            </view>
        </view>
    </view>