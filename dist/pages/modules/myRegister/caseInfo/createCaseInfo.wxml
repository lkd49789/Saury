
    <view class="container">
        <view class="pageTitle">
            <text>案件基本信息</text>
        </view>
        <view class="auditContent">
            
<view>
     <view class="textarea"  wx:if="{{$Name$isShow}}">
        <view>
            <text>{{$Name$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Name$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Name$input.name}}" bindfocus="$Name$bindfocus" bindblur="$Name$bindblur" placeholder="请输入{{$Name$input.title}}" bindinput="$Name$bindinput" maxlength="-1" value="{{$Name$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Name$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Name$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Name$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Name$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Name$input.name}}" bindfocus="$Name$bindfocus" bindblur="$Name$bindblur" placeholder="请输入{{$Name$input.title}}" bindinput="$Name$bindinput" disabled="{{$Name$input.disabled}}" value="{{$Name$inputValue}}" type="{{$Name$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Name$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

            <view class="flex-container">
                
    <view class="option">
        <view>
            <text class="title">{{$Category$options.title}}</text>
        </view>
        <picker bindchange="$Category$bindPickerChange" value="{{$Category$index}}" range="{{$Category$options.displayText}}" name="{{$Category$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Category$options.displayText[$Category$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Category$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$Partition$options.title}}</text>
        </view>
        <picker bindchange="$Partition$bindPickerChange" value="{{$Partition$index}}" range="{{$Partition$options.displayText}}" name="{{$Partition$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Partition$options.displayText[$Partition$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Partition$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

            </view>
            <view class="tui-picker-content"  wx:if="{{Category.value[CategoryIndex]=='FS'||Category.value[CategoryIndex]=='ZS'}}">
                <view>案件业务子类</view>
                <picker mode="multiSelector" value="{{SubCategoryIndex}}" range="{{SubCategory.displayText}}" bindcolumnchange="bindcolumnSubCategoryChange" bindchange="dateChange" data-wpydatechange-a="CACT">
                    <view class="tui-picker-detail">
                        <text>{{SubCategory.content.length!==0?SubCategory.content:'请选择'}}</text>
                        <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                    </view>
                </picker>
            </view>
            <view  wx:if="{{Category.value[CategoryIndex]=='XS'||Category.value[CategoryIndex]=='MS'||Category.value[CategoryIndex]=='XZ'}}">
                
    <view class="option">
        <view>
            <text class="title">{{$Stage$options.title}}</text>
        </view>
        <picker bindchange="$Stage$bindPickerChange" value="{{$Stage$index}}" range="{{$Stage$options.displayText}}" name="{{$Stage$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Stage$options.displayText[$Stage$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Stage$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

            </view>
            <view class="flex-container">
                
    <view class="option">
        <view>
            <text class="title">{{$OrganizationUnitId$options.title}}</text>
        </view>
        <picker bindchange="$OrganizationUnitId$bindPickerChange" value="{{$OrganizationUnitId$index}}" range="{{$OrganizationUnitId$options.displayText}}" name="{{$OrganizationUnitId$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$OrganizationUnitId$options.displayText[$OrganizationUnitId$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$OrganizationUnitId$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                <view class="tui-picker-content">
                    <view>收案日期</view>
                    <picker id="AcceptDateText" mode="date" value="{{submitData.AcceptDateText}}" start="2000-01-01" end="2050-12-31" bindchange="bindAcceptDateChange" style="flex:1">
                        <view class="tui-picker-detail">
                            {{submitData.AcceptDateText||'请选择'}}
                            <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                        </view>
                    </picker>
                    <view  wx:if="{{AcceptDateWarning}}" style="color:#e20000">
                        <text>此字段是必填字段</text>
                    </view>
                </view>
            </view>
            <view class="flex-container">
                
    <view class="option">
        <view>
            <text class="title">{{$Language$options.title}}</text>
        </view>
        <picker bindchange="$Language$bindPickerChange" value="{{$Language$index}}" range="{{$Language$options.displayText}}" name="{{$Language$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Language$options.displayText[$Language$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Language$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
    <view class="option">
        <view>
            <text class="title">{{$SecretLevel$options.title}}</text>
        </view>
        <picker bindchange="$SecretLevel$bindPickerChange" value="{{$SecretLevel$index}}" range="{{$SecretLevel$options.displayText}}" name="{{$SecretLevel$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$SecretLevel$options.displayText[$SecretLevel$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$SecretLevel$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

            </view>
            <view class="checked-box">
                <view>
                    <checkbox id="" value="{{IsForeign.checked}}" checked="{{IsForeign.checked}}" bindtap="IsForeign" color="#5d73fa"/>{{IsForeign.title}}
                </view>
                <view>
                    <checkbox id="" value="{{IsLegal.checked}}" checked="{{IsLegal.checked}}" bindtap="Legal" color="#5d73fa"/>{{IsLegal.title}}
                </view>
                <!-- <view>
                    <checkbox id="" value="{{ImportLevel.checked}}" checked="{{ImportLevel.checked}}" bindtap="isImportLevel" color="#5d73fa" />{{ImportLevel.title}}
                </view> -->
            </view>
            
    <view class="option">
        <view>
            <text class="title">{{$ImportLevel$options.title}}</text>
        </view>
        <picker bindchange="$ImportLevel$bindPickerChange" value="{{$ImportLevel$index}}" range="{{$ImportLevel$options.data}}" range-key="{{$ImportLevel$options.key}}" name="{{$ImportLevel$options.name}}" disabled="{{$ImportLevel$options.disabled}}">
            <view class="picker">
                <view>
                    <text>{{$ImportLevel$options.data[$ImportLevel$index][$ImportLevel$options.key]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$ImportLevel$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

            <view class="flex-container"  wx:if="{{Category.value[CategoryIndex]=='FG'}}">
                <view class="tui-picker-content">
                    <view>开始日期</view>
                    <picker id="StartDate" mode="date" value="{{submitData.StartDate}}" start="2000-01-01" end="2050-12-31" bindchange="bindStartDateDateChange" style="flex:1">
                        <view class="tui-picker-detail">
                            {{submitData.StartDate||'请选择'}}
                            <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                        </view>
                    </picker>
                </view>
                <view class="tui-picker-content">
                    <view>结束日期</view>
                    <picker id="EndDate" mode="date" value="{{submitData.EndDate}}" start="2000-01-01" end="2050-12-31" bindchange="bindEndDateDateChange" style="flex:1">
                        <view class="tui-picker-detail">
                            {{submitData.EndDate||'请选择'}}
                            <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                        </view>
                    </picker>
                </view>
            </view>
            <view class="tui-picker-content" wx:if="{{Category.value[CategoryIndex]=='XS'||Category.value[CategoryIndex]=='MS'||Category.value[CategoryIndex]=='XZ'}}">
                <view>案由分类</view>
                <picker mode="multiSelector" value="{{ReasonIndex}}" range="{{Reason.displayText}}" bindcolumnchange="bindcolumnReasonChange" bindchange="dateChange" data-wpydatechange-a="CAAY">
                    <view class="tui-picker-detail">
                        <text>{{Reason.content.length!==0?Reason.content:'请选择'}}</text>
                        <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                    </view>
                </picker>
            </view>
            <view wx:if="{{Category.value[CategoryIndex]=='XS'||Category.value[CategoryIndex]=='MS'||Category.value[CategoryIndex]=='XZ'}}">
                
<view>
     <view class="textarea"  wx:if="{{$ReasonSupplement$isShow}}">
        <view>
            <text>{{$ReasonSupplement$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$ReasonSupplement$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$ReasonSupplement$input.name}}" bindfocus="$ReasonSupplement$bindfocus" bindblur="$ReasonSupplement$bindblur" placeholder="请输入{{$ReasonSupplement$input.title}}" bindinput="$ReasonSupplement$bindinput" maxlength="-1" value="{{$ReasonSupplement$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$ReasonSupplement$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$ReasonSupplement$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$ReasonSupplement$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$ReasonSupplement$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$ReasonSupplement$input.name}}" bindfocus="$ReasonSupplement$bindfocus" bindblur="$ReasonSupplement$bindblur" placeholder="请输入{{$ReasonSupplement$input.title}}" bindinput="$ReasonSupplement$bindinput" disabled="{{$ReasonSupplement$input.disabled}}" value="{{$ReasonSupplement$inputValue}}" type="{{$ReasonSupplement$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$ReasonSupplement$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

            </view>
            <view wx:if="{{Category.value[CategoryIndex]=='ZC'}}">
                
<view>
     <view class="textarea"  wx:if="{{$MattersEntrusted$isShow}}">
        <view>
            <text>{{$MattersEntrusted$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$MattersEntrusted$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$MattersEntrusted$input.name}}" bindfocus="$MattersEntrusted$bindfocus" bindblur="$MattersEntrusted$bindblur" placeholder="请输入{{$MattersEntrusted$input.title}}" bindinput="$MattersEntrusted$bindinput" maxlength="-1" value="{{$MattersEntrusted$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$MattersEntrusted$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$MattersEntrusted$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$MattersEntrusted$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$MattersEntrusted$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$MattersEntrusted$input.name}}" bindfocus="$MattersEntrusted$bindfocus" bindblur="$MattersEntrusted$bindblur" placeholder="请输入{{$MattersEntrusted$input.title}}" bindinput="$MattersEntrusted$bindinput" disabled="{{$MattersEntrusted$input.disabled}}" value="{{$MattersEntrusted$inputValue}}" type="{{$MattersEntrusted$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$MattersEntrusted$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

            </view>
            <view wx:if="{{Category.value[CategoryIndex]=='MS'||Category.value[CategoryIndex]=='XZ'||Category.value[CategoryIndex]=='ZC'}}">
                
    <view class="option">
        <view>
            <text class="title">{{$Agent$options.title}}</text>
        </view>
        <picker bindchange="$Agent$bindPickerChange" value="{{$Agent$index}}" range="{{$Agent$options.displayText}}" name="{{$Agent$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Agent$options.displayText[$Agent$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Agent$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

            </view>
            <!-- <view wx:if="{{Category.value[CategoryIndex]=='XS'||Category.value[CategoryIndex]=='MS'||Category.value[CategoryIndex]=='XZ'||Category.value[CategoryIndex]=='ZC'}}">
                                                        <AgencyAuthority v-bind:options.sync="AgencyAuthority" v-bind:index.sync='AgencyAuthorityIndex' v-bind:twoWayTitle.once="AgencyAuthorityIndex" />
                                                    </view> -->
            <view class="option"  wx:if="{{Category.value[CategoryIndex]=='XS'||Category.value[CategoryIndex]=='MS'||Category.value[CategoryIndex]=='XZ'||Category.value[CategoryIndex]=='ZC'}}">
                <view>
                    <text class="title">代理权限</text>
                </view>
                <picker bindchange="bindPickerChangeAgencyAuthorityData" value="{{AgencyAuthorityData[AgencyAuthorityData.length-1]}}" range="{{AgencyAuthority.displayText}}" name="AgencyAuthority">
                    <view class="picker">
                        <view style="display:flex;flex-wrap: wrap" wx:if="{{AgencyAuthorityData.length!==0}}">
                            <block wx:for="{{AgencyAuthorityData}}" wx:for-index="index" wx:for-item="item" wx:key="index">
                                <view class="part-item">
                                    <text>{{AgencyAuthority.displayText[item]}}</text>
                                    <icon class="iconfont icon-zirenwushanchu1" style="font-size:48rpx" catchtap="deletePItem" data-wpydeletepitem-a="{{index}}"/>
                                </view>
                            </block>
                        </view>
                        <view wx:else>请选择</view>
                        <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
                    </view>
                </picker>
            </view>
            
<view>
     <view class="textarea"  wx:if="{{$Description$isShow}}">
        <view>
            <text>{{$Description$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Description$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Description$input.name}}" bindfocus="$Description$bindfocus" bindblur="$Description$bindblur" placeholder="请输入{{$Description$input.title}}" bindinput="$Description$bindinput" maxlength="-1" value="{{$Description$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Description$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Description$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Description$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Description$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Description$input.name}}" bindfocus="$Description$bindfocus" bindblur="$Description$bindblur" placeholder="请输入{{$Description$input.title}}" bindinput="$Description$bindinput" disabled="{{$Description$input.disabled}}" value="{{$Description$inputValue}}" type="{{$Description$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Description$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

        </view>
        <image src="{{'../../../../images/test.png'}}" mode="scaleToFill" lazy-load="true" class="addImage" style="opacity:{{addOpacity}} ;" bindtouchstart="touchStart" bindtouchend="touchEnd">
        </image>
    </view>