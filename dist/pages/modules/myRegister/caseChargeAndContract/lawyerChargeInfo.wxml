
    <view class="container">
        <view class="pageTitle">
            <text>律师费用信息</text>
        </view>
        <view class="auditContent">
            
    <view class="option">
        <view>
            <text class="title">{{$PaidParty$options.title}}</text>
        </view>
        <picker bindchange="$PaidParty$bindPickerChange" value="{{$PaidParty$index}}" range="{{$PaidParty$options.displayText}}" name="{{$PaidParty$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$PaidParty$options.displayText[$PaidParty$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$PaidParty$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

            <view class="flex-container">
                
<view>
     <view class="textarea"  wx:if="{{$ChargeLimit$isShow}}">
        <view>
            <text>{{$ChargeLimit$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$ChargeLimit$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$ChargeLimit$input.name}}" bindfocus="$ChargeLimit$bindfocus" bindblur="$ChargeLimit$bindblur" placeholder="请输入{{$ChargeLimit$input.title}}" bindinput="$ChargeLimit$bindinput" maxlength="-1" value="{{$ChargeLimit$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$ChargeLimit$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$ChargeLimit$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$ChargeLimit$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$ChargeLimit$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$ChargeLimit$input.name}}" bindfocus="$ChargeLimit$bindfocus" bindblur="$ChargeLimit$bindblur" placeholder="请输入{{$ChargeLimit$input.title}}" bindinput="$ChargeLimit$bindinput" disabled="{{$ChargeLimit$input.disabled}}" value="{{$ChargeLimit$inputValue}}" type="{{$ChargeLimit$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$ChargeLimit$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
    <view class="option">
        <view>
            <text class="title">{{$Currency$options.title}}</text>
        </view>
        <picker bindchange="$Currency$bindPickerChange" value="{{$Currency$index}}" range="{{$Currency$options.displayText}}" name="{{$Currency$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Currency$options.displayText[$Currency$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Currency$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

            </view>
            <view class="flex-container">
                
    <view class="option">
        <view>
            <text class="title">{{$ExchangeRate$options.title}}</text>
        </view>
        <picker bindchange="$ExchangeRate$bindPickerChange" value="{{$ExchangeRate$index}}" range="{{$ExchangeRate$options.displayText}}" name="{{$ExchangeRate$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$ExchangeRate$options.displayText[$ExchangeRate$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$ExchangeRate$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

                
<view>
     <view class="textarea"  wx:if="{{$ExchangeRateValue$isShow}}">
        <view>
            <text>{{$ExchangeRateValue$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$ExchangeRateValue$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$ExchangeRateValue$input.name}}" bindfocus="$ExchangeRateValue$bindfocus" bindblur="$ExchangeRateValue$bindblur" placeholder="请输入{{$ExchangeRateValue$input.title}}" bindinput="$ExchangeRateValue$bindinput" maxlength="-1" value="{{$ExchangeRateValue$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$ExchangeRateValue$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$ExchangeRateValue$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$ExchangeRateValue$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$ExchangeRateValue$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$ExchangeRateValue$input.name}}" bindfocus="$ExchangeRateValue$bindfocus" bindblur="$ExchangeRateValue$bindblur" placeholder="请输入{{$ExchangeRateValue$input.title}}" bindinput="$ExchangeRateValue$bindinput" disabled="{{$ExchangeRateValue$input.disabled}}" value="{{$ExchangeRateValue$inputValue}}" type="{{$ExchangeRateValue$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$ExchangeRateValue$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

            </view>
            <view class="checked-box">
                <checkbox id="IsTarget" value="{{IsTargetChecked}}" checked="{{IsTargetChecked}}" bindtap="IsTarget" color="#5d73fa"/>{{IsTarget.title}}
            </view>
            <view class="flex-container"  wx:if="{{IsTargetChecked}}">
                
<view>
     <view class="textarea"  wx:if="{{$TargetAmount$isShow}}">
        <view>
            <text>{{$TargetAmount$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$TargetAmount$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$TargetAmount$input.name}}" bindfocus="$TargetAmount$bindfocus" bindblur="$TargetAmount$bindblur" placeholder="请输入{{$TargetAmount$input.title}}" bindinput="$TargetAmount$bindinput" maxlength="-1" value="{{$TargetAmount$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$TargetAmount$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$TargetAmount$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$TargetAmount$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$TargetAmount$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$TargetAmount$input.name}}" bindfocus="$TargetAmount$bindfocus" bindblur="$TargetAmount$bindblur" placeholder="请输入{{$TargetAmount$input.title}}" bindinput="$TargetAmount$bindinput" disabled="{{$TargetAmount$input.disabled}}" value="{{$TargetAmount$inputValue}}" type="{{$TargetAmount$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$TargetAmount$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
<view>
     <view class="textarea"  wx:if="{{$TargetProrate$isShow}}">
        <view>
            <text>{{$TargetProrate$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$TargetProrate$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$TargetProrate$input.name}}" bindfocus="$TargetProrate$bindfocus" bindblur="$TargetProrate$bindblur" placeholder="请输入{{$TargetProrate$input.title}}" bindinput="$TargetProrate$bindinput" maxlength="-1" value="{{$TargetProrate$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$TargetProrate$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$TargetProrate$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$TargetProrate$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$TargetProrate$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$TargetProrate$input.name}}" bindfocus="$TargetProrate$bindfocus" bindblur="$TargetProrate$bindblur" placeholder="请输入{{$TargetProrate$input.title}}" bindinput="$TargetProrate$bindinput" disabled="{{$TargetProrate$input.disabled}}" value="{{$TargetProrate$inputValue}}" type="{{$TargetProrate$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$TargetProrate$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

            </view>
            <view class="flex-container">
                
<view>
     <view class="textarea"  wx:if="{{$CostLimit$isShow}}">
        <view>
            <text>{{$CostLimit$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$CostLimit$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$CostLimit$input.name}}" bindfocus="$CostLimit$bindfocus" bindblur="$CostLimit$bindblur" placeholder="请输入{{$CostLimit$input.title}}" bindinput="$CostLimit$bindinput" maxlength="-1" value="{{$CostLimit$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$CostLimit$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$CostLimit$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$CostLimit$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$CostLimit$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$CostLimit$input.name}}" bindfocus="$CostLimit$bindfocus" bindblur="$CostLimit$bindblur" placeholder="请输入{{$CostLimit$input.title}}" bindinput="$CostLimit$bindinput" disabled="{{$CostLimit$input.disabled}}" value="{{$CostLimit$inputValue}}" type="{{$CostLimit$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$CostLimit$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

                
<view>
     <view class="textarea"  wx:if="{{$FreeHours$isShow}}">
        <view>
            <text>{{$FreeHours$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$FreeHours$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$FreeHours$input.name}}" bindfocus="$FreeHours$bindfocus" bindblur="$FreeHours$bindblur" placeholder="请输入{{$FreeHours$input.title}}" bindinput="$FreeHours$bindinput" maxlength="-1" value="{{$FreeHours$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$FreeHours$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$FreeHours$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$FreeHours$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$FreeHours$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$FreeHours$input.name}}" bindfocus="$FreeHours$bindfocus" bindblur="$FreeHours$bindblur" placeholder="请输入{{$FreeHours$input.title}}" bindinput="$FreeHours$bindinput" disabled="{{$FreeHours$input.disabled}}" value="{{$FreeHours$inputValue}}" type="{{$FreeHours$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$FreeHours$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

            </view>
             <view class="checkedBox">
                <view class="title">
                    收费方式
                </view>
                <checkbox-group bindchange="checkboxChange" class="checkbox-group">
                    <label wx:for="{{PayStyle}}" wx:key="index" index="index" item="item " class="label">
                            <checkbox value="{{item.value}}" checked="{{item.isSelected}}" name="id" color="#5d73fa"/> {{item.displayText}}
                        </label>
                </checkbox-group>
            </view>
            <view class="uploadBox">
                <view class="upload">
                    <text>项目合同文件</text>
                    <button class="btn" size="mini" bindtap="upload"> 上传 </button>
                </view>
                <view class="doc" wx:if="{{docShow}}">
                    <view class="content-left">
                        <icon class="iconfont icon-wendang" style="font-size:100rpx;color:#ff9900;margin-right:30rpx;"/>
                    </view>
                    <view class="content-right">
                        <view class="content-left-right" style="width:100%;margin-bottom:22rpx">
                            <text style="margin-bottom:14rpx">{{CaseContractList[0].name}}</text>
                        </view>
                        <view style="display:flex;justify-content:space-between;align-items:center;width:100%;color:#7a7a7a">
                            <view>
                            <text>{{CaseContractList[0].creationTime}} <text>最新上传</text></text>
                            </view>
                            <view>
                            <text>共{{CaseContractList.length}}份文档</text>
                            </view>
                        </view>
                    </view>
                </view>
            </view>
        </view>
        <image src="{{isNextBtn?'../../../../images/next.png':'../../../../images/test.png'}}" mode="scaleToFill" lazy-load="true" class="addImage" bindtouchend="isNextPage"/>
    </view>