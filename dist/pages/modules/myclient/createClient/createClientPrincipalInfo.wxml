
  <view class="container">
    <view class="pageTitle">
      <text  wx:if="{{isShow}}">负责人信息</text>
      <text  wx:else>自然人</text>
    </view>
    <view class="content" wx:if="{{isShow}}">
      
    <view class="option">
        <view>
            <text class="title">{{$LegalPerson$options.title}}</text>
        </view>
        <picker bindchange="$LegalPerson$bindPickerChange" value="{{$LegalPerson$index}}" range="{{$LegalPerson$options.displayText}}" name="{{$LegalPerson$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$LegalPerson$options.displayText[$LegalPerson$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$LegalPerson$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

      
<view>
     <view class="textarea"  wx:if="{{$Linker$isShow}}">
        <view>
            <text>{{$Linker$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Linker$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Linker$input.name}}" bindfocus="$Linker$bindfocus" bindblur="$Linker$bindblur" placeholder="请输入{{$Linker$input.title}}" bindinput="$Linker$bindinput" maxlength="-1" value="{{$Linker$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Linker$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Linker$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Linker$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Linker$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Linker$input.name}}" bindfocus="$Linker$bindfocus" bindblur="$Linker$bindblur" placeholder="请输入{{$Linker$input.title}}" bindinput="$Linker$bindinput" disabled="{{$Linker$input.disabled}}" value="{{$Linker$inputValue}}" type="{{$Linker$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Linker$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

      
    <view class="option">
        <view>
            <text class="title">{{$LegalDuty$options.title}}</text>
        </view>
        <picker bindchange="$LegalDuty$bindPickerChange" value="{{$LegalDuty$index}}" range="{{$LegalDuty$options.displayText}}" name="{{$LegalDuty$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$LegalDuty$options.displayText[$LegalDuty$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$LegalDuty$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

      
<view>
     <view class="textarea"  wx:if="{{$Landline$isShow}}">
        <view>
            <text>{{$Landline$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Landline$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Landline$input.name}}" bindfocus="$Landline$bindfocus" bindblur="$Landline$bindblur" placeholder="请输入{{$Landline$input.title}}" bindinput="$Landline$bindinput" maxlength="-1" value="{{$Landline$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Landline$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Landline$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Landline$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Landline$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Landline$input.name}}" bindfocus="$Landline$bindfocus" bindblur="$Landline$bindblur" placeholder="请输入{{$Landline$input.title}}" bindinput="$Landline$bindinput" disabled="{{$Landline$input.disabled}}" value="{{$Landline$inputValue}}" type="{{$Landline$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Landline$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

      
<view>
     <view class="textarea"  wx:if="{{$Fax$isShow}}">
        <view>
            <text>{{$Fax$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Fax$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Fax$input.name}}" bindfocus="$Fax$bindfocus" bindblur="$Fax$bindblur" placeholder="请输入{{$Fax$input.title}}" bindinput="$Fax$bindinput" maxlength="-1" value="{{$Fax$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Fax$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Fax$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Fax$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Fax$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Fax$input.name}}" bindfocus="$Fax$bindfocus" bindblur="$Fax$bindblur" placeholder="请输入{{$Fax$input.title}}" bindinput="$Fax$bindinput" disabled="{{$Fax$input.disabled}}" value="{{$Fax$inputValue}}" type="{{$Fax$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Fax$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

    </view>
    <view class="content" wx:else>
      
<view>
     <view class="textarea"  wx:if="{{$CardNo$isShow}}">
        <view>
            <text>{{$CardNo$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$CardNo$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$CardNo$input.name}}" bindfocus="$CardNo$bindfocus" bindblur="$CardNo$bindblur" placeholder="请输入{{$CardNo$input.title}}" bindinput="$CardNo$bindinput" maxlength="-1" value="{{$CardNo$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$CardNo$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$CardNo$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$CardNo$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$CardNo$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$CardNo$input.name}}" bindfocus="$CardNo$bindfocus" bindblur="$CardNo$bindblur" placeholder="请输入{{$CardNo$input.title}}" bindinput="$CardNo$bindinput" disabled="{{$CardNo$input.disabled}}" value="{{$CardNo$inputValue}}" type="{{$CardNo$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$CardNo$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

      
    <view class="option">
        <view>
            <text class="title">{{$Nation$options.title}}</text>
        </view>
        <picker bindchange="$Nation$bindPickerChange" value="{{$Nation$index}}" range="{{$Nation$options.displayText}}" name="{{$Nation$options.name}}">
            <view class="picker">
                <view>
                    <text>{{$Nation$options.displayText[$Nation$index]||'请选择'}}</text>
                </view>
                <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
            </view>
        </picker>
         <view  wx:if="{{$Nation$options.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>

      <view class="tui-picker-content">
        <view>出生日期</view>
        <picker id="Birthday" mode="date" value="{{submitData.Birthday}}" start="2000-01-01" end="2050-12-31" bindchange="bindDateChange" style="flex:1">
          <view class="tui-picker-detail">
            {{submitData.Birthday||'请选择'}}
            <icon class="updown iconfont icon-gengduoneirongdianjichakanxiangqing" style="font-size:30rpx;color:#b2b2b2"/>
          </view>
        </picker>
      </view>
      
<view>
     <view class="textarea"  wx:if="{{$Occupation$isShow}}">
        <view>
            <text>{{$Occupation$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Occupation$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Occupation$input.name}}" bindfocus="$Occupation$bindfocus" bindblur="$Occupation$bindblur" placeholder="请输入{{$Occupation$input.title}}" bindinput="$Occupation$bindinput" maxlength="-1" value="{{$Occupation$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Occupation$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Occupation$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Occupation$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Occupation$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Occupation$input.name}}" bindfocus="$Occupation$bindfocus" bindblur="$Occupation$bindblur" placeholder="请输入{{$Occupation$input.title}}" bindinput="$Occupation$bindinput" disabled="{{$Occupation$input.disabled}}" value="{{$Occupation$inputValue}}" type="{{$Occupation$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Occupation$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

    </view>
    <view style="padding:0 28rpx 30rpx 28rpx">
      
<view>
     <view class="textarea"  wx:if="{{$Email$isShow}}">
        <view>
            <text>{{$Email$input.title}}</text>
        </view>
        <view class="textarea-container" style="border-bottom:1px solid {{$Email$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <textarea name="{{$Email$input.name}}" bindfocus="$Email$bindfocus" bindblur="$Email$bindblur" placeholder="请输入{{$Email$input.title}}" bindinput="$Email$bindinput" maxlength="-1" value="{{$Email$inputValue}}" auto-height cursor-spacing="5px" fixed="{{$Email$true}}" style="width:83%"/>
        </view>
        <view wx:if="{{$Email$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
    <view class="input"  wx:else>
        <view>
            <text>{{$Email$input.title}}</text>
        </view>
        <view class="input-container" style="border-bottom:1px solid {{$Email$inputChecked?'#5d73fa':'#dedede'}};color:#7a7a7a">
            <input name="{{$Email$input.name}}" bindfocus="$Email$bindfocus" bindblur="$Email$bindblur" placeholder="请输入{{$Email$input.title}}" bindinput="$Email$bindinput" disabled="{{$Email$input.disabled}}" value="{{$Email$inputValue}}" type="{{$Email$input.type||'text'}}" confirm-type="done" cursor-spacing="5px"/>
        </view>
        <view wx:if="{{$Email$input.warning}}" style="color:#e20000">
                <text>此字段是必填字段</text>
        </view>
    </view>
   
</view>
    

    </view>
    <image src="{{'../../../../images/test.png'}}" mode="scaleToFill" lazy-load="true" class="addImage" style="opacity:{{addOpacity}} ;" bindtouchstart="touchStart" bindtouchend="touchEnd">
    </image>
  </view>